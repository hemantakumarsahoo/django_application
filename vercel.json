{
    "builds": [{
        "src": "web_app/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "web_app/wsgi.py"
        }
    ]
}
